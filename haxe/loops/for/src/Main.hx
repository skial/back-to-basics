package ;

class Main {

    public static function main() {
        var chars = ['h', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd'];
        for (i in 0...chars.length) {
            Sys.print(chars[i]);
        }
    }

}